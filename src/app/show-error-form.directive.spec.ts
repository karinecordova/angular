import { ShowErrorFormDirective } from './show-error-form.directive';

describe('ShowErrorFormDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowErrorFormDirective();
    expect(directive).toBeTruthy();
  });
});
