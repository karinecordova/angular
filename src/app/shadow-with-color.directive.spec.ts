import { ShadowWithColorDirective } from './shadow-with-color.directive';

describe('ShadowWithColorDirective', () => {
  it('should create an instance', () => {
    const directive = new ShadowWithColorDirective();
    expect(directive).toBeTruthy();
  });
});
